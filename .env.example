# Supabase (create table with schema/supabase_products_table.sql first)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-or-anon-key

# Scraper (EU embeds full product JSON; use eu for reliable scraping)
BASE_URL=https://www.miumiu.com
MARKET=en
COUNTRY=eu
# Optional: set to 1 to only save JSON and skip Supabase upload
DRY_RUN=0
# Optional: max products to scrape (0 = no limit)
# LIMIT=5
